import com.sun.media.sound.SoftShortMessage
public class Shop implements Observer {
    @Override
    public void update(Subject subject, Object state) {
        if (subject instanceof Storage storage) {

        }
    }
}